<div class="container" style="padding:20px 0">
    <h1>Users</h1>

    <div class="container">
        <div class="row">
            <div class="col-sm-6">
            <table class="table table-striped table-hover">
                <thead>
                    <tr><th>ID</th><th>name</th><th>mail address</th></tr>
                </thead>
                <tbody>
                    <% @users.each do |u| %>
                        <% i = 0 %>
                        <% if i < 3 %>
                        <tr><td><%= u.id %></td><td><%= u.name %></td><td><%= u.mail_address %></td></tr>
                    <% end %>        
                </tbody>
            </table>
            </div>
            <div class="col-sm-6">
            <table class="table table-striped table-hover">
                <thead>
                    <tr><th>ID</th><th>name</th><th>mail address</th></tr>
                </thead>
                <tbody>
                    <% @users.each do |u| %>
                        <tr><td><%= u.id %></td><td><%= u.name %></td><td><%= u.mail_address %></td></tr>
                    <% end %>        
                </tbody>
            </table>
            </div>
        </div>
    </div>

</div>
